let userObj = {
    name: 'lisi',
    age: 20,
    sayHi(){
        console.log('hello nodejs');
    }
}

module.exports.userObj = userObj;

//npm init --yes会强制生成一个package.json,将当前模块作为入口文件"main": "config.js"

//如果当前模块在根目录下不存在，则会去node_modules中去找，而不用写.js后缀名